# Personal Cable TV

Personal Cable TV turns a Jellyfin library into scheduled, linear cable-style Live TV channels.

Setup files are being added now. The recommended install will be a prebuilt Docker image plus a CasaOS import file so users do not need to build the app in Terminal.
